# Unresolved blockers / open items

1. **`phase-1-email-scope.md` missing** from the workspace (`references/` only had ASSETS/audits stubs at inspection time). Scope was taken from this task brief + Figma annotations + production templates.
2. **Social icon CDN assets** not wired in registration-approval footer (text placeholders only).
3. **Credit card fee row** exists in `festival_donation` production payload but not on Figma EN donation frame — omitted pending design decision.
4. **Ticket-sale specialized commerce modules** (room booking, add-ons, inline donation) exist in production template but not on Figma Ticket/Pass reference — omitted from preview HTML.
5. **Staff recipient variants** (admin/organizer) for registration approval and ticket sale deferred (Figma notes “variants deferred to QA frames”).
6. **Apple Wallet / Google Wallet / ICS** are link + callout fixtures only — no `.pkpass` / calendar bytes generated (by design).
7. **Localized copy for AR/FA** in preview is approximate for non-activate strings where full yaml parity was not re-imported for every commerce string; activate EN matches `en.yaml`.
8. **No formal email-client matrix** run — browser screenshots only.
9. Designs remain **DRAFT / AWAITING APPROVAL** — do not mark APPROVED.
